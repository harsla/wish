{ "name": "wish-framework",
  "version":  "0.0.0",
  "dependencies": {
    "underscore": "1.5.2",
    "express": "3.4.4",
    "async": "0.2.9",
    "jayschema": "0.2.0",
    "mysql": "2.0.0"
  },
  "devDependencies": {
    "mocha": "1.14.0",
    "should": "2.1.1"
  },
  "engines": {
    "node": "0.10.22",
    "npm":  "1.3.14"
  },
  "scripts": {
    "unit-test": "mocha --recursive --ui tdd test/unit",
    "integration-test": "mocha --recursive --ui tdd test/integration",
    "full-test": "mocha --recursive --ui tdd test",
    "test": "npm run-script unit-test"
  }
}